apiVersion: v1
kind: Service
metadata:
  name: {{ include "book-store-chart.fullname" . }}
  labels:
    {{- include "book-store-chart.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "book-store-chart.selectorLabels" . | nindent 4 }}
